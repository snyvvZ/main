import React from "react";
import TitlePage from "../common/TitlePage";

const Coordi = () => {
  return (
    <section>
      <TitlePage page={`코디`} />
    </section>
  );
};

export default Coordi;
