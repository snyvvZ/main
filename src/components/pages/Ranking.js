import React from "react";
import TitlePage from "../common/TitlePage";

const Ranking = () => {
  return (
    <section>
      <TitlePage page={`랭킹`} />
    </section>
  );
};

export default Ranking;
